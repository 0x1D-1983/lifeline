apiVersion: v1
kind: Service
metadata:
  name: {{ include "lifeline.fullname" . }}
  labels:
    {{- include "lifeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lifeline.selectorLabels" . | nindent 4 }}
