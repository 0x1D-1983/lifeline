apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lifeline.fullname" . }}-test-connection"
  labels:
    {{- include "lifeline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lifeline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
